import SwiftUI

// TODO: [Phase 3] Implement OptionDetailView — placeholder content.
// TODO: [Phase 4] Add WebPreviewView + LogOutputView split.
// See agents.md Prompts 6, 7.

struct OptionDetailView: View {
    var body: some View {
        VStack {
            Spacer()
            Text("Select an option to view its preview and logs.")
                .foregroundColor(.secondary)
            Spacer()
        }
        .frame(maxWidth: .infinity, maxHeight: .infinity)
    }
}
