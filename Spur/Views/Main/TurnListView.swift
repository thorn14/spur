import SwiftUI

// TODO: [Phase 5] Implement TurnListView — turns/checkpoints + fork button.
// See agents.md Prompt 8.

struct TurnListView: View {
    var body: some View {
        List {
            Text("No turns yet.")
                .foregroundColor(.secondary)
        }
    }
}
