import SwiftUI

// TODO: [Phase 6] Implement CommandRunnerView — command input + streaming output.
// See agents.md Prompt 9.

struct CommandRunnerView: View {
    var body: some View {
        VStack {
            Text("Command Runner")
                .foregroundColor(.secondary)
        }
        .frame(maxWidth: .infinity, maxHeight: .infinity)
    }
}
