import SwiftUI

// TODO: [Phase 3] Implement ExperimentListView — see agents.md Prompt 6.

struct ExperimentListView: View {
    var body: some View {
        List {
            Text("No experiments yet.")
                .foregroundColor(.secondary)
        }
    }
}
